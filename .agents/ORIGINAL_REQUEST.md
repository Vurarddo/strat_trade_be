# Original User Request

## 2026-08-28T11:42:13Z

Perform a **brutal, uncompromising critical stress-test** of an autonomous binary options trading bot (Pocket Option AutoTrader Pro). The bot trades on Pocket Option, including OTC (synthetic, broker-generated) assets 24/7. Your job is to find every logic hole, mathematical flaw, strategy weakness, and architectural vulnerability that could cause the bot to systematically lose money.

**This is a pure research and analysis task — produce a detailed Markdown report. No code changes.**

Working directory: /Users/vlados/work/projects/startup/strat_trade_be

Integrity mode: development

## Context & Reference Materials

The team MUST read these files to understand the full bot architecture before starting analysis:

**Core Engine (read ALL of these):**
- `src/strat_trade/domain/trading/bot_engine.py` — Main 932-line `LiveDemoBotEngine` (trading loop, settlement, circuit breakers, signal evaluation, order execution)
- `src/strat_trade/domain/trading/entities.py` — All domain dataclasses (`BotStatus`, `TradeOutcome`, `PreTradingPlan`, `LiveTradeRecord`, `BotSessionSummary`)
- `src/strat_trade/domain/trading/regime_detector.py` — Market regime detection (ADX, EMA ribbon, ATR)
- `src/strat_trade/domain/trading/asset_filter.py` — Toxic blacklist, session filter, microstructure quality gates
- `src/strat_trade/domain/trading/correlation.py` — Currency correlation & directional exposure filter
- `src/strat_trade/domain/trading/trade_store.py` — SQLite persistence (WAL mode)

**Strategy Layer (read ALL of these):**
- `src/strat_trade/domain/strategies/base.py` — `BaseStrategy` ABC + `SignalResult`
- `src/strat_trade/domain/strategies/registry.py` — 8-strategy registry
- ALL strategy implementations in `src/strat_trade/domain/strategies/` (read every .py file)

**Optimizer:**
- `src/strat_trade/domain/optimizer/auto_matcher.py` — `StrategyAutoMatcher` with quantum score formula and micro-backtesting

**Use Cases:**
- `src/strat_trade/use_cases/auto_assign_strategies.py` — PreTradingPlan generation
- `src/strat_trade/use_cases/manage_live_bot.py` — Bot lifecycle singleton

**Adapter:**
- `src/strat_trade/adapters/pocket_option_gateway.py` — WebSocket adapter for Pocket Option

**Skill Reference Materials** (domain knowledge for the analysis team — read these for expertise context):
- `.agents/skills/business-analyst/SKILL.md` — Binary options EV math, payout sensitivity, statistical significance
- `.agents/skills/risk-manager/SKILL.md` — Kelly criterion, circuit breakers, position sizing, streak analysis
- `.agents/skills/market-analyst/SKILL.md` — Regime detection, OTC mechanics, session dynamics, correlation
- `.agents/challenger_m1_1/skills/quant-strategy-researcher.md` — Strategy research methodology, overfitting prevention

## Requirements

### R1. Comprehensive Critical Stress-Test Report

Produce an exhaustive, brutally honest analysis report covering these 4 mandatory analysis axes PLUS additional findings:

**Axis 1: Short-Expiry Noise Sensitivity**
- Analyze how each of the 8 strategies performs on M1 timeframe with 180s (3-bar) default expiration
- Identify which indicator configurations are mathematically fragile on 1-minute candles (e.g., RSI(14) on M1 = only 14 minutes of history, ADX(14) = 14 minutes for trend detection)
- Calculate the theoretical signal-to-noise ratio degradation at M1 vs M5/M15 for each indicator used
- Evaluate whether the 0.50 confidence threshold is sufficient to filter noise on short expirations
- Assess expiration mismatch risk: when does the predicted move NOT complete within 180 seconds?

**Axis 2: Mathematical Expectancy (EV) at 75-85% Payouts**
- Calculate exact breakeven win rates for the full payout range 70%-92%
- For each strategy, estimate realistic achievable win rate on OTC M1 data and compute the EV per dollar staked
- Build a sensitivity matrix showing how a 2-3% drop in win rate (due to OTC noise or slippage) affects profitability
- Identify the "death zone" — the payout level below which the bot's current strategy mix becomes mathematically guaranteed to lose
- Analyze the compounding effect: how does negative EV on even ONE strategy drag down portfolio performance over 500+ trades?
- Calculate the probability of ruin given current position sizing (flat $10 or 1% of balance) over various trade counts

**Axis 3: OTC Algorithmic Spike Vulnerability**
- Analyze how OTC (broker-generated synthetic) pricing differs from real interbank feeds
- Identify which indicators are most vulnerable to OTC-specific artifacts:
  - Discrete price steps (flat bars with 0 range)
  - Artificial wicks / pin bars generated by the broker's pricing algorithm
  - Sudden synthetic reversals ("snapping back") that invalidate trend indicators
  - Step-function price movements that create false breakout signals
- Evaluate whether the current microstructure quality gate (4 metrics: flat_bar_ratio, unique_price_ratio, whipsaw_sign_flip_ratio, relative_atr) is sufficient to catch OTC manipulation patterns
- Propose additional OTC-specific filters that are missing

**Axis 4: Overfitting & Signal Queue Conflicts**
- Analyze the `StrategyAutoMatcher` quantum score formula for overfitting risks:
  - Only 150 candles = ~2.5 hours of M1 data for backtesting — is this sufficient?
  - Priority strategy bonus (+15) and whitelist bonus (+15) bias the selection toward specific strategies regardless of actual performance
  - Are the parameter variations generated by `_generate_strategy_variations()` sufficient to avoid local optima?
- Identify signal queue race conditions:
  - What happens when multiple strategies fire signals for the same asset simultaneously?
  - Analyze the `_order_lock` — does it prevent all double-entry scenarios?
  - Can the 4-second tick loop miss signal expiration (signal valid at t=0 but market changed by t=4)?
- Evaluate look-ahead bias risk in the backtest engine
- Assess whether the 30-second signal-to-signal cooldown is too short or too long

**Additional Critical Analysis:**
- Circuit breaker edge cases (can the bot oscillate between PAUSED and RUNNING in rapid succession?)
- Post-settlement cooldown gaps (is `max(180s, cooldown_bars × 60s)` correct?)
- Correlation filter completeness (does it catch ALL major currency exposure overlaps?)
- Regime detector blind spots (what happens in regime transition zones when ADX ≈ 24?)
- SQLite trade_store atomicity under concurrent writes from multiple settled trades
- The observed anomaly in the database: 10 trades opened in <3 seconds, all CALL, bypassing all cooldowns — root cause analysis

### R2. Monte Carlo Worst-Case Simulation

Build theoretical Monte Carlo simulation models (described in the report, not code) for:
- 10,000 synthetic 500-trade sequences using the bot's actual parameters (flat $10 stake, 80% payout, estimated 55-60% win rate per strategy)
- Calculate: probability of ruin, expected max drawdown distribution, 95th percentile worst streak length
- Model the compounding effect of payout fluctuations (payout randomly varying between 72% and 88% per trade)
- Simulate the impact of OTC noise: add ±2% random win-rate degradation per 50-trade block to model regime shifts
- Present results as statistical tables with confidence intervals

### R3. Prioritized Remediation Roadmap

For each vulnerability found:
- Severity classification: 🔴 CRITICAL (will cause losses), 🟡 HIGH (likely causes losses under stress), 🟢 MEDIUM (suboptimal but survivable)
- Estimated impact on overall win rate (in percentage points)
- Recommended fix with clear technical specification
- Priority ranking for implementation (P0 = fix before going live, P1 = fix within first week, P2 = optimize later)

## Acceptance Criteria

### Analysis Depth
- [ ] All 8 strategy implementations have been read and individually analyzed
- [ ] Every gate in the 11-step signal evaluation pipeline has been evaluated for bypasses or gaps
- [ ] The quantum score formula has been mathematically decomposed and bias sources identified
- [ ] OTC-specific vulnerabilities are grounded in concrete examples from the code, not generic warnings

### Mathematical Rigor
- [ ] Breakeven win rate tables are computed for payouts 70%, 75%, 80%, 85%, 90%, 92%
- [ ] EV calculations show exact formulas with worked examples
- [ ] Monte Carlo parameters match the bot's actual configuration (from `entities.py` and `bot_engine.py`)
- [ ] Probability of ruin calculations use proper gambler's ruin / Kelly framework

### Completeness
- [ ] The report contains >= 15 distinct vulnerabilities/findings
- [ ] Each finding has a severity classification and remediation recommendation
- [ ] The database anomaly (10 trades in <3 seconds) has a root-cause explanation
- [ ] The report addresses ALL 4 axes explicitly requested by the user

### Actionability
- [ ] Remediation roadmap has clear P0/P1/P2 priority levels
- [ ] Each fix recommendation is specific enough that a developer could implement it
- [ ] The report identifies the single most impactful change that would improve the bot's profitability
